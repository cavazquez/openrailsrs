# Fixtures ETCS DMI

Subset of Open Rails `Content/ETCS` symbols (ERA ERTMS 015560) used by unit tests and as fallback when no OR install is found.

Full set: Open Rails `Source/RunActivity/Content/ETCS` or installed `Content/ETCS`.

Override: `OPENRAILSRS_ETCS_CONTENT=/path/to/ETCS`.

Credit: see `ETCS_DMI_symbols_credit.txt`.
