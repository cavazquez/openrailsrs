[package]
name = "openrailsrs-audio"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Synthesized audio engine for openrailsrs (cab sounds + ambient regions)"

[dependencies]
rodio = "0.21.1"
